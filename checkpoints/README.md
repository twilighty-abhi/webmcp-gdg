# Checkpoints

`01-first-tool` registers only `searchEvents`. `02-multiple-tools` adds details and registration. `03-final-example` is the polished reference implementation with safety annotations and structured errors. Each checkpoint keeps its `registerTool` calls directly in `main.js`, so participants can compare the progression without hunting through shared utilities. Serve the repository root and open each path directly.
